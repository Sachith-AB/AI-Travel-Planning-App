import { Text, View } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        flex: 1
      }}
    >
      <Text style={{
        fontSize:30,
        fontFamily:'outfit'
      }}>Home Page</Text>
    </View>
  );
}
